# Python
__pycache__/
*.pyc

# Large model weights — distributed via Google Drive (see stage2/download_weights.sh)
*.pth

# Training-only pretrained inits (not needed for inference)
pretrained_model/

# Inference outputs & Pass-1 caches
inference_results/**/pred_*.nii.gz
inference_results/**/*_pred.nii.gz
inference_results/**/*.npy
inference_results/**/_pass1_cache.npz
